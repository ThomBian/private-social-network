# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Profile {
  id: ID!
  bio: String!
  userId: ID!
  user: User!
}

type Connection {
  status: String!
  group: String!
}

type User {
  id: ID!
  username: String!
  email: String!
  phoneNumber: String!
  createdAt: DateTime!
  profile: Profile
  posts: [Post!]
  connectionToMe: Connection
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Post {
  id: ID!
  img: String!
  post_date: DateTime!
  size: PostSize!
  type: String!
  caption: String!
  author: User!
  authorId: ID!
  audience: PostAudience!
}

enum PostSize {
  rectangle
  square
}

enum PostAudience {
  FAMILY
  FRIENDS
  ALL
}

type AuthPayload {
  token: String!
  user: User!
}

type Query {
  users: [User!]!
  user(id: ID!): User
  userByUsername(username: String!): User
  profile(userId: ID!): Profile
  feed: [Post!]!
  profilePosts(username: String!): [Post!]!
  post(id: String!): Post
}

type Mutation {
  requestPhoneVerificationCode(phoneNumber: String!): Float!
  loginWithPhoneCode(phoneNumber: String!, code: Float!): AuthPayload!
  requestFollow(ownerId: String!, viewerId: String!): Boolean!
  approveFollow(ownerId: String!, viewerId: String!, group: String!): Boolean!
  declineFollow(ownerId: String!, viewerId: String!): Boolean!
  updateProfile(userId: ID!, bio: String!): Profile!
  createPost(caption: String!, img: String!, size: String!, type: String!): Post!
  deletePost(id: String!): Post!
}